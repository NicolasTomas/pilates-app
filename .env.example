## Copy this file to `.env` and fill the values below.
## For local MongoDB use: mongodb://localhost:27017
## For MongoDB Atlas use the SRV string from Atlas UI (Drivers):
## MONGO_URI=mongodb+srv://<username>:<password>@<cluster-host>/?retryWrites=true&w=majority&appName=<ClusterName>
## IMPORTANT: If your password has special characters, URL-encode them (e.g., @ -> %40, # -> %23).

MONGO_URI=mongodb://localhost:27017
DB_NAME=pilatesdb
JWT_SECRET=change_this_secret
PORT=3000
